export interface SidebarItem {
    title: string;
    icon?: string;
    path: string;
}

export interface HomeCard {
    icon: string;
    title: string;
    description?: string;
    path: string;
}